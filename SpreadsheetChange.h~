/* SpreadsheetChange.h
 * 
 * Represents a single change within the spreadsheet server.
 * Contains a cell name, a previous value, and a new value.
 *
 * Author: Adam Waggoner
 */

#include <string>

public:
  SpreadsheetChange(char* cell, char* prev, char* new);
   char* _cell_name; 
  char* _prev, _next;
